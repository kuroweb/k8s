apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-migrate-deployment
spec:
  template:
    spec:
      containers:
        - name: backend-migrate
          command:
            - "/bin/sh"
            - "-c"
            - "RAILS_ENV=production rails db:create && RAILS_ENV=production rails db:migrate && ./bin/rails server -b 0.0.0.0 -p 3000 -e production"
          env:
            - name: DD_SERVICE
              value: "price-monitoring"
            - name: DD_ENV
              value: "production"
