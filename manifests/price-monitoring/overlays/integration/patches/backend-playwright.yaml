apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-playwright-deployment
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: backend-playwright
          resources:
            limits:
              cpu: "300m"
              memory: "500Mi"
