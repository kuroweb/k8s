apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-provider-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kuroweb/k8s.git
    targetRevision: HEAD
    path: manifests/auth-provider/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: auth-provider-production
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
